import React from "react";

import { Background, Main } from "./Cart.elements";

export default function Cart({ handleClose, cartOpen }) {
  return (
    // <Background onClick={handleClose} className={cartOpen ? "fade-in-bkg" : ""}>
    //   <Main onClick={e => e.stopPropagation()}></Main>
    // </Background>
    <></>
  );
}
